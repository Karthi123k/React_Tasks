Use Effect is a hook for rendering some functionality after rendering the whole page. In this function dependency array[] once declare only render then when the change that particular thing.Once remove the array render every time.
App Layout is a functional component.

Component in component is not good and also react also not suggested.
Don’t write a usestate inside the if condition and also for also it’s leads inconsistency.
Usesate never use outside of the functional component.
We can use more useeffect in the in single js file.
Why cdn images store is the best for the hosted. Is fast.

When want some repeated content in that time 
{Array(10).fill(“”).map((e,index)=>
(
console.log(“hello”);
))}
Formik is best for forms when u used complex forms in react.

Finding a path.
Library react router

How to instal react router  npm i react-router-dom
NiBefore that import the react-router-dom.
Import {creatBrowserRouter, Router provider} from "react-router-dom";


Create about page after
If inthe path way error occur then create error page export and import the page while error occurred.
errorElement :<Error/>
In the error page import the useRoyteErorr for the react-router-dom.
Const err=useRouteErorr();
Console.log(err); and also print {err.status+":"+ errStatusText}
And destructing the err like
 const 

Routing is 2 types client,server.
Now I want develop spa_single page application.
React is cilent side routing 
Anchor alternative 
Link. Form react-router-dom use same as anchor tag herf alternative is to

Outlet
Get Childerns of the applayout 
Import the outlet form react-routing-dom
Outlet is placeholder  Childerns name replace. Complete outlet is used for daynamic pages purpose.


Dynamic segments means dynamic routing
  Useparams -> is hook
Carry one-page parameters to another page
